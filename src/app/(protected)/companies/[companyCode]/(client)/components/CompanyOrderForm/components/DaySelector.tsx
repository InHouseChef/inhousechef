export const DaySelector = () => {
    // return (
    //     <div
    //         onClick={() => onClick(index)}
    //         className={clsx('cursor-pointer rounded-full px-3 py-1', isSelected ? 'bg-blue-500 text-white' : 'bg-gray-200')}>
    //         {index + 13} {day}
    //     </div>
    // )
}

export default DaySelector
